# Instalación: pip install -r requirements.txt
# Alternativa recomendada: pip install -e ".[dev]"

# --- API ---
fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12

# --- Modelos y configuración ---
pydantic>=2.9
pydantic-settings>=2.6

# --- Cliente LLM (Ollama por HTTP) ---
httpx>=0.27

# --- Motor terminológico ---
# La recuperación pesada la hace FTS5, dentro de SQLite. rapidfuzz sólo
# puntúa las decenas de candidatos que FTS5 devuelve.
rapidfuzz>=3.10

# --- Persistencia ---
# SQLite va en la biblioteca estándar: sin dependencia y sin servidor.

# --- Documentos ---
reportlab>=4.2
pypdf>=5.1

# --- Protocolos clínicos ---
# El frontmatter de las skills es YAML.
pyyaml>=6.0

# --- Utilidades ---
dateparser>=1.2
